spring:
  application:
    name: patient-mgmt
    
server:
  port: {$SERVER_PORT:8080} # Default port is 8080
  
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html