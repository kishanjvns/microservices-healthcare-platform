apiVersion: v1
kind: Service
metadata:
  name: appointment-mgmt-service
spec:
  type: NodePort
  selector:
    app: appointment-mgmt
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
