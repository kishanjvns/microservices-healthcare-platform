global:
  postgresql:
    auth:
      existingSecret: postgres-default-secret
      secretKeys:
        adminPasswordKey: postgres-password 
        userPasswordKey: appointment-mgmt-password
      username: appointment_user
      database: appointment_db
      password: Password1
    image:
      tag: 18.2.0    
primary:
  persistence:
    enabled: false